# write debuger.py (PowerShell here-string writes only the Python code)
@'
import pickle
m = pickle.load(open(r"F:\Python\FastAPI01\model.pkl","rb"))
print(type(m))
print(m.__class__.__module__, m.__class__.__name__)
print([a for a in dir(m) if not a.startswith("_")])
'@ | Set-Content -Path debuger.py -Encoding UTF8

# run it with venv python
& .\myenv\Scripts\python.exe .\debuger.py